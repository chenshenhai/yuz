{
  "private": true,
  "scripts": {
    "init": "lerna bootstrap --npm-client=cnpm",
    "test": "lerna bootstrap --no-ci && lerna run build && lerna run test",
    "test:yuz": "lerna bootstrap --no-ci && lerna run build && lerna run --scope yuz test",
    "test:local": "lerna bootstrap --npm-client=cnpm && lerna run build && lerna run test:local"
  },
  "devDependencies": {
    "@octokit/core": "^3.2.4",
    "lerna": "^3.22.1",
    "react": "^17.0.1",
    "react-dom": "^17.0.1",
    "typescript": "^4.1.3"
  }
}

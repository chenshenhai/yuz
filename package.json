{
  "private": true,
  "scripts": {
    "test:all": "lerna bootstrap --no-ci && lerna run build && lerna run test",
    "test": "lerna bootstrap && lerna run --scope yuz test"
  },
  "devDependencies": {
    "@octokit/core": "^3.2.4",
    "lerna": "^3.22.1",
    "react": "^17.0.1",
    "react-dom": "^17.0.1",
    "typescript": "^4.1.3"
  }
}
